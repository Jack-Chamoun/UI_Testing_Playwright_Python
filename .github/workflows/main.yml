name: CI Pipeline - Pytest in Docker

on:
  push:
    branches:
      - main
jobs:
  playwright-tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build image
        run: docker build -t playwright-testing-pipeline .

      - name: Run tests
        run: |
          docker run \
            -v ${{ github.workspace }}:/app \
            playwright-testing-pipeline

      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: report
          path: reports/report.html
